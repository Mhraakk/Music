"use client";

/**
 * Sleeve colour stays on the artwork. Paper + one ink stamp.
 * A room wash would add a second chromatic voice.
 */

export function RoomTint({ searchColor: _searchColor }: { searchColor?: string | null }) {
  return null;
}
