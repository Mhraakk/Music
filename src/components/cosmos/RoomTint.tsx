"use client";

/**
 * Sleeve colour stays on the artwork. The Telepathic surface is linen + one
 * orange CTA — a room wash would add a second chromatic voice.
 */

export function RoomTint({ searchColor: _searchColor }: { searchColor?: string | null }) {
  return null;
}
